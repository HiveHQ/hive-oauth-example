# Hive OAuth Examples

Simple OAuth 2.0 examples showing how a basic UI connects to Hive's OAuth server and initiates the Authorization Code flow.

## Available examples

- `examples/node` — Node.js backend example
- `examples/python` — Python backend example
